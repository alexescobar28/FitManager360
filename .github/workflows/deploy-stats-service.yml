name: Deploy Stats Service

on:
  push:
    branches: [main, master]
    paths:
      - 'backend/stats-service/**'
      - '.github/workflows/deploy-stats-service.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/stats-service/package-lock.json

      - name: Install dependencies
        run: |
          cd backend/stats-service
          npm ci

      - name: Run tests (if any)
        run: |
          cd backend/stats-service
          npm test --if-present

      - name: Deploy to Render
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: ${{ secrets.RENDER_STATS_SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}
